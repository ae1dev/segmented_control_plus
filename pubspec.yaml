name: segmented_control_plus
description: "A clean simple segmented control widget that works on all platforms."
version: 0.0.2
homepage: https://github.com/ae1dev/segmented_control_plus

# This package supports all platforms listed below.
platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:

topics:
  - ui
  - widget
  - button

environment:
  sdk: ^3.5.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:

